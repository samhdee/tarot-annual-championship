$(function () {
    // Status actif
    // $(document).on('change', '#user-list-table input[name="active_state"]', function (e) {
    //     const active_switch = $(e.target);
    //
    //     $.post('/admin/user/active', {
    //         user_id: $(active_switch).parents('tr').data('user_id'),
    //         new_state: $(active_switch).is(':checked') ? 1 : 0,
    //     });
    // });
    //
    // // Admin
    // $(document).on('change', '#user-list-table input[name="admin_state"]', function (e) {
    //     const active_switch = $(e.target);
    //
    //     $.post('/admin/user/admin', {
    //         bga_user_id: $(active_switch).parents('tr').data('bga_user_id'),
    //         new_state: $(active_switch).is(':checked') ? 1 : 0,
    //     });
    // });
});
